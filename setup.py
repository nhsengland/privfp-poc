"""Setup file for privacy_fingerprint.

Please note that setup.cfg is used for configuration.
"""
from setuptools import setup

setup(
    # For compatibility, see pyproject.toml for settings
    setup_requires=["setuptools_scm"],
)
